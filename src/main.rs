mod detect_staves;

use image::{open};

fn main() {

    //let image = ImageReader::open("src/staff_images/empty_staff.png").unwrap().in

    // let image = open("src/staff_images/empty_staff.png").unwrap().as_luma8().unwrap();

    // for row in image.rows() {
        
    // }

    // for (x, y, rgb) in image.pixels() {
    //     let gray_scale = 
    //         (0.3 * rgb[0] as f32 + 0.6 * rgb[1] as f32 + 0.1 * rgb[2] as f32) as u8;
        

    // }

}
